services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: "1234"
      MYSQL_DATABASE: "uci_projects"
    ports:
      - "3307:3306"
  
  app: 
    build: .
    depends_on:
      - db